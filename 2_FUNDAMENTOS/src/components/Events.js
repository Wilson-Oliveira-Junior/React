const Events = () => {

    const handleMyEvent = () => {

    }

    return (<div>
        <div>
            <button onClick={handleMyEvent()}>Clique Event!</button>
        </div>
    </div>
    );
};

export default Events;